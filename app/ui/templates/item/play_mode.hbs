<div class="background" style="background-image: url({{imageForResourceScale img}})"></div>

<div class="sliding-panel-content">
	{{#if isAvailableToday}}
		{{#unless isAvailableTommorrow}}
			<div class="availability-info">
				<div>{{localize 'common.play_mode_open_for_label' name=name}}</div>
				<div class="countdown-available-for"></div>
			</div>
		{{/unless}}
	{{else}}
		<div class="availability-info">
			<div>{{localize 'common.play_mode_available_on_label'}} <strong>{{getNextAvailableDate}}</strong></div>
			<div class="countdown-until-available"></div>
		</div>
	{{/if}}
	{{#if softAvailableOnDate}}
			<div class="availability-info">
          <div>{{localize 'common.play_mode_available_on_label'}} <strong>{{getNextAvailableDate}}</strong></div>
          <div class="countdown-until-available"></div>
			</div>
	{{/if}}
	{{#if softDisableOnDate}}
      <div class="availability-info">
          <div>{{localize 'common.play_mode_open_for_label' name=name}}</div>
          <div class="countdown-available-for"></div>
      </div>
	{{/if}}
	<div class="name">{{name}}</div>
	<div class="description">
		<hr class="shine">
		<div>{{description}}</div>
	</div>
</div>

<div class="locked-label"><i class="fa fa-lock"></i> {{#if unlockMessage}}{{{unlockMessage}}}{{else}}{{localize 'common.generic_not_available_message'}}{{/if}}</div>

<!--
<div class="sliding-panel-active-content"></div>
-->
